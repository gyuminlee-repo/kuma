# Handover: 2026-05-12-1056

## 현재 상태 (1-2문장)
kuma i18n(eng/kor) 변환 목표 진행 중. Batch 1 (ParameterPanel/DesignReport/DiversitySections/PolymeraseEditor)은 이미 변환 완료 상태로 확인, Batch 2 (MAME widgets + popovers 8개) 작업을 @frontend 에이전트에 디스패치한 직후 세션이 인계됨.

## 완료된 작업
- [x] /goal 활성화: "kuma 다중언어(eng/kor) 완벽 변환 진행"
- [x] Baseline audit
  - 인프라 확인: `src/lib/i18n.ts` (react-i18next, localStorage `kuma:locale`, en/ko/system)
  - 로케일 파일: `src/locales/en.json`, `src/locales/ko.json` — 171 키, 양쪽 동등 (missing in ko/en = 0)
  - 동일-텍스트 4개(ko=en 미번역): `about.sidecar`, `mame.activity.tabIngest/tabMerge/tabExport`
  - useTranslation 미사용 tsx 파일: 76개 (test 제외)
  - 실제 hardcoded JSX 문자열 보유 파일: 45개 (총 ~180 instances)
- [x] Batch 1 확인: ParameterPanel/DesignReport/DiversitySections/PolymeraseEditor 모두 이미 t() 변환 완료, en/ko 키 등록됨 (parameterPanel.* 57키 포함). 추가 작업 불요.
- [x] TaskCreate 6건 등록 (id 1~6)

## 진행 중 / 미완료
- [ ] **Batch 2 (in_progress)**: MAME widgets + popovers 8파일 (~61 strings). @frontend 에이전트 디스패치 직후 세션 종료 — 결과 미수신.
  - RunHealthPanel(15), PlateView(9), OffTargetDetail(8), VerdictTable(7), HairpinDetail(5), SummaryRow(5), PlateMap(6), JobQueuePanel(6)
- [ ] Batch 3: MAME panels + dialogs (InputPanel 8, ExportDialog 6, JanusMappingDialog 6, RunReportDialog 6, MameAppLayout 4, WtWellEditor 3, ActivityUploadPanel 2)
- [ ] Batch 4: kuro panels + screens + layout (SequenceInput 7, MutationInput 5, Home/MainShell/Onboarding 각 5, AppLayout 6, GlobalAppBar/Status 각 3, UniprotSearch 3, DiversityOptions 3, 기타 dialog)
- [ ] 동일-텍스트 4개 ko 보정: `about.sidecar`, `mame.activity.tabIngest/tabMerge/tabExport`
- [ ] 최종 검증: `npx tsc --noEmit` + 로컬 빌드

## 핵심 결정 & 이유
- **인프라 신규 구축 안 함**: i18next + react-i18next 이미 설정 완료 (`src/lib/i18n.ts`, 의존성 `i18next ^26.0.10`, `react-i18next ^17.0.7`). 키 추가와 t() 배선만 필요.
- **네임스페이스 컴포넌트별 분리**: 기존 패턴(`parameterPanel.*`, `mame.*`)을 따라 새 파일은 컴포넌트명 네임스페이스로. 반복 라벨은 `common.*` 재사용.
- **regex 오탐 발견**: 초기 audit에서 `>[A-Z]...<` 패턴이 t() 내부 문자열도 매칭. ParameterPanel 등 4개는 false positive였음. 2차 audit (useTranslation 미사용 파일만)에서 76개로 좁힘.
- **Batch 분할 (밀도 기준)**: 디스패치 실패 회피를 위해 처음에 1파일 단위로 축소 → ParameterPanel 확인 성공 후 8파일 묶음으로 확장.

## 주의사항 & 교훈
- @frontend Agent 호출이 첫 시도에 실패함 (Batch 1 통째). 1파일 단위로 재시도 후 정상 동작. **8파일 묶음 Batch 2도 실패 위험** — 결과 미확인 상태.
- audit 패턴은 `useTranslation` 미사용 + JSX 문자열 보유 양쪽 조건으로 좁혀야 false positive 회피.
- en.json 동등 키 4개(`about.sidecar`, `mame.activity.tab*`)는 단순 "Sidecar:"·"1. Ingest" 같은 영어 유지 의도일 수 있음 — ko 번역 적용 전 의도 확인 권장.
- 도메인 용어(SDM, primer, plasmid, barcode, throughput, verdict, well, plate, cross-talk) 한국어 번역은 음차 또는 영어 유지 — 격언 톤·overstatement 금지, em dash 금지.

## 다음 세션 시작점
1. **Batch 2 결과 수신 확인**:
   - `git status` + `git diff src/locales/` 로 Batch 2 변경 여부 확인
   - 변경 없으면 Batch 2 재디스패치 (1~2파일씩 더 작게)
2. **검증**: `cd $REPO_ROOT && npx tsc --noEmit 2>&1 | tail -30` (repo = cc/kuma)
3. **Batch 3 디스패치** (MAME panels/dialogs 7파일, ~35 strings)
4. **Batch 4 디스패치** (kuro panels + screens + layout, ~60 strings)
5. **마무리**: 동일-텍스트 4개 ko 보정 → tsc 재검증 → `/goal complete` 감사
6. 커밋: `vX.X.X: complete eng/kor i18n coverage for remaining UI components` (semver MM 또는 CC 증가)

## 중요 파일
| 파일 | 역할 | 상태 |
|------|------|------|
| `src/lib/i18n.ts` | i18next 초기화, locale 슬롯 | 변경 없음 |
| `src/locales/en.json` | 영문 번역 (171키) | Batch 2 추가 진행 중 |
| `src/locales/ko.json` | 한글 번역 (171키, 4개 미번역) | Batch 2 추가 + 4키 보정 필요 |
| `src/components/layout/MenuBar.tsx` | 참조 패턴 (useTranslation 사용 예) | 변경 없음 |
| `/tmp/no-i18n2.txt` | useTranslation 미사용 파일 76개 목록 | 세션 종료로 휘발성 |
| `notes/handoffs/handoff-unknown-2026-05-12-1053.md` | auto-handoff 훅 산출 | 미작성 (Context/Decisions 비어있음) |
