#!/usr/bin/env bash
# Local pre-push gate. Mirrors what tag-only CI used to catch on every push.
# Skip in emergencies: git push --no-verify
set -e

echo "[pre-push] pnpm sync:check"
pnpm sync:check

echo "[pre-push] npx tsc --noEmit"
npx tsc --noEmit

echo "[pre-push] ok"
