# Handover: 2026-05-12-1040

## 현재 상태
kuma 다중 언어(eng/kor) 완전 변환 목표 설정 + i18n 베이스라인 감사 완료. Batch 1 실행 직전 단계에서 컨텍스트 클리어 됨. 코드 변경 0건 (audit only).

## 완료된 작업
- [x] `/goal clear` 후 신규 목표 등록: "kuma의 다중 언어 지원을 일단 eng/kor부터 완벽하게 변환되게"
- [x] i18n 인프라 확인: `src/lib/i18n.ts` (react-i18next, localStorage `kuma:locale`, en/ko/system 분기) 존재
- [x] 의존성 확인: `i18next ^26.0.10`, `react-i18next ^17.0.7` package.json 등록됨
- [x] 로케일 파일 감사: `src/locales/en.json` & `ko.json` 모두 171 keys, missing 0건
- [x] 미번역 동등 키 4건 식별: `about.sidecar`, `mame.activity.tabIngest/Merge/Export` (ko가 en과 동일)
- [x] 비-i18n 컴포넌트 식별: 102개 tsx 중 83개가 `useTranslation` 미사용. 목록 `/tmp/no-i18n.txt` 저장 (재생성 필요할 수 있음 — 휘발성).
- [x] 하드코딩 문자열 밀도 랭킹 산출 (상위 40개 파일)
- [x] TaskCreate로 6개 배치 등록 (#1 완료, #2 in_progress)

## 진행 중 / 미완료
- [ ] **Batch 1 (in_progress)**: 4 고밀도 파일 — 합 ~136 strings
  - `src/components/panels/ParameterPanel.tsx` (37)
  - `src/components/dialogs/DesignReport.tsx` (37)
  - `src/components/panels/InputPanel/DiversitySections.tsx` (32)
  - `src/components/dialogs/PolymeraseEditor.tsx` (30)
  - @frontend 에이전트에 디스패치 메시지 작성 완료, 그러나 컨텍스트 클리어 시점에 실제 에이전트 응답/실행 결과 미수신.
- [ ] Batch 2: popovers(Candidate/FailedMutation/OffTarget/Hairpin) + RunHealthPanel + ManifestDiffDialog (~75)
- [ ] Batch 3: MAME 패널/다이얼로그 (InputPanel, PlateView, VerdictTable, Export/Janus/RunReport, MameAppLayout, SummaryRow) (~50)
- [ ] Batch 4: screens + layout 잔여 (Home, MainShell, Onboarding, AppLayout, GlobalAppBar/StatusBar, kuro InputPanel/*, ReRunManifestDialog)
- [ ] 동등-텍스트 4개 ko 보정 + `npx tsc --noEmit` 빌드 검증

## 핵심 결정 & 이유
- **i18next 라이브러리 유지**: 이미 설치·초기화 완료. 마이그레이션 비용 무가치.
- **네임스페이스 컴포넌트별 분기**: `parameterPanel.*`, `designReport.*` 등. 이유 — 기존 키 네임스페이스(menu/file/export/edit/help/about/settings/mame/common)와 충돌 회피 + 컴포넌트별 추적 가능.
- **밀도 기반 배치 순서**: 고밀도 파일부터 처리 → 진척률 가시화 + 사용자 가시성 큰 화면 우선.
- **번역만 + 코드 직접 작성 금지**: @frontend 에이전트 위임 (CLAUDE.md @lead 규칙).

## 주의사항 & 교훈
- `/tmp/no-i18n.txt` 휘발성 — 세션 재개 시 재생성 명령 필요:
  ```bash
  find src -name "*.tsx" ! -name "*.test.tsx" | xargs grep -L "useTranslation\|i18n" 2>/dev/null > /tmp/no-i18n.txt
  ```
- ko 번역 시 em dash(—) 금지, possessive 's 금지, overstatement 회피 (CLAUDE.md 작문 규칙).
- 도메인 용어(SDM, Gibson Assembly, primer, plasmid)는 영어 유지 또는 음차.
- @frontend 위임 시 한 번에 4파일 처리는 토큰 부담 큼 — 응답 못 받았으면 2+2로 쪼개기 고려.

## 다음 세션 시작점
1. `/goal status` 확인 — 활성 목표 이어받기
2. Task 목록 확인 (TaskList) — Batch 1 상태 in_progress
3. **Batch 1 재디스패치**: @frontend 에이전트에 동일 프롬프트 재전송. 프롬프트는 본 핸드오버의 "처리 대상" 4파일 + `src/lib/i18n.ts` 패턴(`src/components/layout/MenuBar.tsx` 참조) 명시.
4. 결과 수신 후 `npx tsc --noEmit` 검증 → commit `v0.3.17: i18n batch 1 (ParameterPanel/DesignReport/DiversitySections/PolymeraseEditor)`
5. Batch 2 → 3 → 4 순차 진행
6. 마지막에 ko 4건 보정 + 전체 빌드 검증 후 `/goal complete`

## 중요 파일
| 파일 | 역할 | 상태 |
|------|------|------|
| `src/lib/i18n.ts` | i18next 초기화 + locale 슬롯 | 완성, 미수정 |
| `src/locales/en.json` | 영문 (171 keys) | 키 확장 예정 |
| `src/locales/ko.json` | 한국어 (171 keys) | 키 확장 + 4건 보정 예정 |
| `src/components/layout/MenuBar.tsx` | i18n 사용 레퍼런스 패턴 | 미수정 |
| `src/components/panels/ParameterPanel.tsx` | Batch1 #1 (37 strings) | 미수정 |
| `src/components/dialogs/DesignReport.tsx` | Batch1 #2 (37 strings) | 미수정 |
| `src/components/panels/InputPanel/DiversitySections.tsx` | Batch1 #3 (32 strings) | 미수정 |
| `src/components/dialogs/PolymeraseEditor.tsx` | Batch1 #4 (30 strings) | 미수정 |
| `package.json` | i18next 26.0.10, react-i18next 17.0.7 | 미수정 |
